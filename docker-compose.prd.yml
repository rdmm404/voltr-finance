services:
  bot:
    container_name: app-voltr-finance
    build:
      context: .
      dockerfile: Dockerfile
      target: final
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
    env_file:
      - .env
    depends_on:
      - db
    networks:
      - postgres-network
  migrate:
    image: ghcr.io/amacneil/dbmate
    command: ["up"]
    volumes:
      - ./db:/db:rw
    depends_on:
      - db
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
    env_file:
      - .env
    networks:
      - postgres-network
    profiles:
      - migrate
volumes:
  db_data:
networks:
  postgres-network:
    external: true
